{
  "modbus-sim": {
    "profileName": "modbus-sim-profile",
    "protocols": {
      "modbus-tcp": {
        "Address": "${MODBUS_DEVICE_ADDRESS}",
        "Port": "${MODBUS_DEVICE_PORT}",
        "UnitID": 1
      }
    },
    "name": "modbus-sim"
  }
}
